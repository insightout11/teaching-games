# Beta Readiness Checklist — Jul 8, 2026

> **Founding Captain funnel update — Aug 13, 2026.** Ordinary Google signup remains open. The tracked `/beta` application path recruits a high-touch Founding Captain cohort; it is not a global invite gate.

## Founding Captain recruitment funnel

- Use `/beta` as the campaign destination, with stable lowercase links such as `/beta?utm_source=reddit&utm_medium=organic&utm_campaign=founding_captains&utm_content=world_flight_demo_v1`.
- Lifecycle is `applied -> signed_up -> onboarded -> activated -> retained`, separate from cohort state (`active`, `inactive`, or `declined`). Onboarding is manual. Automated activation is an activation candidate: it requires an ended session in a non-demo class with at least one `session_participants` row, but founder confirmation (`classroom_use_confirmed_at`) is still needed to establish real classroom use. Retention requires a second qualifying session on a later date.
- Supabase is the membership and lifecycle source of truth. PostHog receives `beta_page_viewed`, `beta_application_submitted`, `beta_google_signin_started`, and `beta_signup_completed` with campaign fields only—never applicant free text, email, student identity, or session recordings.
- Public application abuse control allows 10 valid attempts per salted IP hash per hour and 500 attempts globally per day. One service-role Postgres RPC holds a global transaction-scoped advisory lock while it purges expired rows, checks both caps, and inserts an allowed attempt, so parallel requests cannot pass stale count checks. Production must provide the already-used `PUBLIC_DEMO_IP_SALT`; the endpoint fails closed if it is missing. Raw IP addresses are never written to the beta attempt table. Hashes older than 30 days are purged on the next atomic claim; without later endpoint traffic, expired rows may remain until another claim triggers cleanup.
- Privacy decision (Aug 15, 2026): the operator name is Lesson Captain, the privacy contact is `beta@lessoncaptain.com`, initial recruitment is open to online English teachers worldwide, and beta application/program records are retained for up to 12 months after application or the most recent beta interaction before deletion or anonymization, subject to limited security, legal, or unresolved-request exceptions.
- After lockfile-based dependency repair, run the read-only founder export from a normal repository shell with `pnpm beta:export`. The command explicitly loads `.env.local` through Node's `--env-file` option and writes a timestamped CSV to `C:\Users\insig\Documents\Codex\_outputs`; pass arguments after `--`, such as `pnpm beta:export -- --output=<path>` or `--include-notes`. Existing output is never overwritten unless `--force` is explicit. The script never writes lifecycle changes.
- Founder exports contain personal data. Keep them outside the repository and never attach them to public issues, marketing assets, or shared support threads.
- Onboarding, lifecycle reconciliation writes, follow-ups, testimonial permission, migration application, deployment, and PostHog dashboard setup remain manual and approval-gated.

Master list of what's left before/around beta. Decisions baked in (owner, Jul 8):
**beta = tracked Founding Captain cohort with ordinary signup still open · beta is free (Stripe after) · sound v1 after beta starts · onboarding = guided phone-join + teacher-screen coaching.**

Agent lanes (owner, Jul 8): **Fable** = hardest/most important coding only · **Opus** = front-end, majority of coding · **Codex** = long backend runs, anything World Flight · **Sonnet** = easy/quick/cheap.

Status legend: each item lists owner lane, size, and whether it blocks beta.

---

## A. Beta blockers (do first, in this order)

**Status (Jul 9): A1 ✅ · A2 ✅ · A3 ✅ · A4 ✅ · A5 ✅ (code) — beta-blocker code is complete.**
Open verification items (owner, ~10 min total on the deployed build):
- **A1 live check (re-run after the Jul 8 pm follow-up fix):** launch Flash Quiz, 30s timer, phone joined → 3-2-1 beat on both screens; **teacher and phone timers now agree within a tick** (the earlier ~2s teacher-trails-student gap is fixed), and the phone answer window should open at **~27–30s**, not the earlier 22s. See the A1 timer-budget note below for why.
- **A2 live check:** launch a timed teacher round with a joined phone → the phone should receive the new round by realtime push in under ~1s; poll fallback remains at 15s and should recover after a refresh/offline blip.
- **A4 live check:** create a course with description "animals" with real AI (Codex's local run had no API key) → every suggestion animal-related or blank.
- **A5(b):** subscribe to Supabase Pro **the week beta invites go out** (not needed sooner — project is active daily so no auto-pause risk yet).

**A1 timer budget (why the numbers are what they are).** Both screens now derive remaining time from the same server timestamps: the answer window runs for `timerSeconds` starting at `answersOpenAt` (= `startedAt` + a 4s grace), via one shared `computeTimerState`. Two consequences:
- **Teacher vs. student agree within a tick** — neither counts from its own local clock anymore; both use `answersOpenAt + timerSeconds − serverNow`. (The teacher's `serverNow` comes from the input-spec API echo; the student's from the poll.)
- **The grace defers the countdown instead of eating it.** The 4s grace absorbs delivery latency up to 4s, so only delivery *beyond* 4s costs answer time. Pre-A2 delivery = DB write + up to a 5s poll interval + poll route, so the residual loss is ~0–3s → students see **~27–30s** of a 30s window (the earlier 22s came from the countdown starting at `startedAt`, i.e. the grace was being eaten — now fixed). The student console logs the exact per-round delivery/loss (`[timer] round arrived …`). **A2 (realtime push, <1s delivery) removes the residual entirely** → a full 30s every time. So A1's job here is done; A2 closes the last ~0–3s and matters more for scale than for this timer.

| # | Item | Lane | Size | Notes |
|---|---|---|---|---|
| A1 | ✅ **DONE** (Jul 8, `82172895` + `79fb3219` + `d65b6e35` + pm follow-up) **Timer integrity** — `maxDuration=60` on 49 AI routes (incl. course/outline gap closure); server-stamped `startedAt`/`answersOpenAt` with `clientStartedAt` round nonce so mid-round rebroadcasts don't reset timers; `serverNow` clock-offset in student poll + `useInputTimer`; 3-2-1 grace beat on all 5 timed input types + flash-quiz teacher screen; speed bonus measures from answers-open. **Follow-up (pm):** unified both screens on one `computeTimerState` (origin = `answersOpenAt + timerSeconds`) so teacher & student agree within a tick and the grace defers the countdown; teacher timers (flash-quiz, synonym-showdown, grid-rush, listening-gap-fill, all 4 race-mode games via `useSyncedTimer`/`use-race-mode`) now read the server clock echoed by the input-spec API | **Fable** | done | Pending: owner live-flow re-check (above) |
| A2 | ✅ **DONE** (Jul 8, Codex code): **Realtime push + poll diet** — per-session inputSpec realtime broadcast; student poll 5s→15s fallback; inputSpec revision short-circuit on the poll route | **Codex** | done | Pending: owner live-flow re-check on deploy (above). Depends on A1's server-time stamping |
| A3 | ✅ **DONE** (Jul 8/9, `44f51517` + `60cbf23c` + `f4dbeb05`): **First-flight onboarding** — 6a interactive phone-join lobby coach for first ~3 sessions; 6b teacher-screen stage coaching with dismissible one-line "what you do now" hints under the live Captain's Flight header plus a cockpit-exists nudge for new teachers. Both are gated by prior session count with `?coach=1` dev override. | **Opus** | done | Covers BOTH sides: phone-join proves the student loop; coaching teaches the cockpit. Demo crew stays dead/unmarketed |
| A4 | ✅ **DONE** (Jul 8, Codex: `46e61b34` + `7ea6b901` + `f49a80a5`) **Course Builder source matching fix** — see status note below | **Codex** | done | Pending: live AI outline acceptance on deploy (above) |
| A5 | ✅ **DONE** (Jul 8, code pushed as `4426c93c`): `/home` credits via RPC (also fires the trickle server-side), world-flight-hero hydration fixes, mock-client rpc stub. (b) Supabase Pro = **calendar item for invite week** | **Codex/Sonnet** | done | 052 verified at schema level day of apply |

A2 status note (Jul 8, Codex): teacher inputSpec writes now compute a stable `inputSpecRevision`, echo server time, and broadcast `{ spec, inputSpecRevision, serverNow }` on `session-input-spec:<sessionId>` after the DB write succeeds. Student clients subscribe to that per-session channel for near-immediate round delivery, keep the 15s poll as fallback, and pass their last revision to `/api/student/session`; the route returns a cheap `{ unchanged: true }` payload while active when the spec has not changed, avoiding participant/poll/board/score reads. Files: `src/lib/input-spec.ts`, `src/app/api/session/input-spec/route.ts`, `src/app/api/student/session/route.ts`, `src/components/student/student-controller.tsx`, `src/stores/session-store.ts`, `src/lib/mock/client.ts`, `src/__tests__/lib/input-spec-stamp.test.ts`, `src/__tests__/api/student-session-realtime.test.ts`. Evidence: A2-focused Vitest tests pass, focused lint passes, `pnpm exec tsc --noEmit --pretty false` passes; full `pnpm test` is blocked by the pre-existing aggregate timeout in `src/lib/world-flight/expeditions.test.ts`, which passes in isolation.

A4 status note (Jul 8, Codex): outline lessons now require 2-4 concrete `keywords`; Course Builder matches on those keywords plus the course theme as domain context; source matching uses whole-word/guarded-prefix token hits with tag/title weighting and a minimum score that returns `suggestedSource: null` below the bar. Follow-up hardening after screenshot acceptance: object inputs with empty keywords no longer fall back to full topic text, generic lesson-action keywords are ignored, cross-domain keywords such as human body language/growth mindset no longer beat the course theme, Business English interview tags now cover STAR answers/interviewer questions/mock interviews/difficult questions/resumes/cover letters, and Travel English transportation tags now cover local transport, bus tickets, train stations/journeys, taxis, planning, and directions. Added `scripts/enrich-library-topic-tags.ts` for local JSON tag enrichment without runtime transcript fetches, and enriched the animal-heavy NatGeo/TED-Ed entries needed for the "animals" repro. Files: `src/app/api/course/outline/route.ts`, `src/lib/source-library.ts`, `src/lib/course.ts`, `src/lib/source-library.test.ts`, `src/__tests__/api/course-outline.test.ts`, `scripts/enrich-library-topic-tags.ts`, `src/data/natgeo-library.json`, `src/data/teded-library.json`, `src/data/business-english-library.json`, `src/data/travel-english-library.json`, `package.json`. Evidence: focused Vitest matcher/API tests pass; deterministic acceptance maps screenshot-style animal lessons only to animal-related sources/null, screenshot-style "job interviews" lessons to Business English, and "transportation" Easy/8 lessons to public transport or directions. Live AI outline acceptance is blocked locally because no Gemini/Groq/OpenAI API key is configured.

## B. Strongly recommended before beta (not strictly blocking)

| # | Item | Lane | Size | Notes |
|---|---|---|---|---|
| B1 | **Perceived speed** — perf plan Phase 3. **Loader ✅ SHIPPED** (Opus): shared aviation `GenerationLoader` (`src/components/ui/generation-loader.tsx`, self-skips <~400ms, side-view plane taxi→liftoff) rolled across all generation-wait spinners in 13 games/activities + `GenerationDegradedNote` for `degraded:true`. **Prefetch DEFERRED to Phase 4/C7** (owner Jul 9): generate-on-selection risks wasted AI cost; per-game round N+1 prefetch is delicate (stale-dedup) and several games already queue — do it measured against beta traffic. | **Opus** | done (loader) | First-impression work; matters more now that trials include custom topics + sources (cache-skipping = 2–8s dead time) |
| B2 | ✅ **DONE** (Jul 9): **One-to-one leftovers** — owner confirmed complete. Covers the one-to-one-audit recs 3–6 scope: explicit 2+ gating where partner mechanics require it, solo-aware leaderboard/result framing, conversation-rounds self-pairing behavior/variant, and softer n=1 copy. | **Sonnet** (3, 6) / **Opus** (4, 5) | done | Recs 1–2 were already shipped via class-fit work |

## C. During/after beta

| # | Item | Lane | Size | Notes |
|---|---|---|---|---|
| C1 | **Sound v1** — 3 SFX (brand resolve, takeoff, touchdown) + global mute + `/dev/sounds` board + **resolve legacy dings** (7 games play unmuted `correct/wrong.mp3` on the teacher screen — delete or fold under mute, default off) | **Opus** (code) + owner/Codex (Ableton assets) | afternoon once files exist | Doc updated Jul 8: `docs/sound-design.md`. Assets are the long pole |
| C2 | **Stripe go-live** — products ($79/yr, $8/mo), env vars, mount `ProCta`, webhook endpoint, Vercel Pro, honor founding-price deadline decision | **Sonnet** (mount/config) + owner (Stripe dashboard) | half day | `docs/billing-setup.md` — steps already updated for $8 |
| C3 | **Marketing Agent OS wiring** — Phase 0 repo (`~/Documents/lessoncaptain-marketing`) is SHIPPED (playbook, 5 charters, ops scaffolding). Remaining: MiniPC agents live (Max dispatcher + Telegram gateway, Hermes research schedules), weekly rhythm running | **Codex** (MiniPC/OpenClaw/Nous side) | multi-day | Parallel track — doesn't touch product repo |
| C4 | **Classes/browse uplift** — `docs/classes-browse-uplift-plan.md`, entirely unimplemented (C1–C3 living class cards + hub, B1–B4 browse redesign) | **Opus** | 3–4 days | Owner-rated weakest pages; promote to pre-beta only if testers will live in /classes |
| C5 | **Library expansion** — NOT a priority as bulk expansion (owner, Jun). The A4 tag-enrichment pass covers quality. When prioritized: automate candidate discovery (topic → <7min captioned candidates → existing free pipeline), human keeps appropriateness sign-off | **Codex** | scriptable | Revisit when beta content gaps show up in requests |
| C6 | **Course Builder v1.1** — source-briefing grounding (reuse planner source panel / extract+briefing); vocab carry-forward; template course seeds (needs owner theme picks) | **Codex** | 2–3 days | After A4 proves matching |
| C7 | **Performance Phase 4** — model tuning: pull a week of `ai_call` logs first; Groq-first experiment for evaluation/game-logic; per-task-class model field; merged auth RPC; prewarm cron; bundle audit | **Codex** | measure-first | Explicitly data-gated — don't start before beta traffic exists |
| C8 | **SEO remainder** — Phase 3 long tail + Phase 4 (`docs/seo-page-template.md` doesn't exist yet) | **Sonnet** | ~1 day | Long-game; not beta-relevant |
| C9 | **Travel directions-game v2** (pair info-gap) + **Captain's Flight Stage 7** (mid-lesson beat parity outside World Flight) | **Codex** (World Flight lane) | 1–2 days each | Codex owns World Flight per owner |

## D. Parked (post-beta, needs product decisions first)

- **Flight Cards** — Scoring V2 dependency is now met (V2 SHIPPED May 2026 — migration 031, `score-engine.ts`, profiles on all plugins; the "parked" label was a stale index entry). Still parked on product design, not tech.
- **End-of-session celebration + teams** (session-level aggregation) · **Student results retention/share** (debrief_token) · **Class Logbook** (locked framing) · **Video Series tab** (curate toward, don't build) · **Lesson composition NL vision** (North Star) · **Turbulence live wiring** (pending owner approval of feel) · **New preset set design** (beyond the 5 shipped).

---

## Suggested sequencing (weeks-away beta)

~~Week 1: A1 · A2 · A3 · A4 · A5 · B1 · B2~~ — **beta-blocker/recommended code completed Jul 9**.
Next up: recruit testers once owner live-flow checks pass. C-items run during beta; C1 whenever Ableton assets exist; C2 when conversion starts.
Owner verification queue: A1 + A2 + A4 live checks on the deployed build (see top of section A); Supabase Pro at invite week.

C5/C6 direction note (Jul 8, owner + Codex): course expansion should be teacher-facing **course presets**, not bulk random library growth. V1 target = 8 editable six-lesson course presets: Travel/Cities/Culture, Food Around the World, Animals & Nature, Climate & Environment, Technology & AI, Job Interview English, Health & Wellbeing, Stories/Myths/Folktales. World Flight's reviewed videos should all become Course Builder library sources, but match only when relevant (cities, cuisine, travel, transport, culture, geography, etc.). External expansion should favor best-of-the-best reputable sources and, where possible, consistent series for continuity.

C5/C6 implementation note (Jul 9, Codex local WIP): added the first course-preset slice and library expansion: 8 editable course presets, all reviewed World Flight videos as a new `world-flight` library source, saved-course source grounding via library `SourceMaterial` resolution, and a first curated Job Interview source batch from BBC Learning English / British Council. Added narrow `--only=` support to the transcript prefetch script so new entries can be backfilled without a full-library run. Files: `src/lib/course-presets.ts`, `src/lib/library-source-material.ts`, `src/data/world-flight-library.json`, `src/data/business-english-library.json`, `src/components/course/course-builder.tsx`, `src/app/api/course/route.ts`, `src/lib/source-library.ts`, `src/lib/source-context.ts`, `scripts/generate-world-flight-library.ts`, `scripts/prefetch-library-transcripts.ts`, plus wiring/tests. Evidence: focused Vitest suite (`course-presets`, `library-source-material`, `source-library`, `source-context`, `course-outline`) passes; touched-file `next lint` passes; `pnpm exec tsc --noEmit --pretty false` passes.

C5/C6 food batch note (Jul 9, Codex local WIP): added six BBC Learning English / 6 Minute English food sources to `src/data/bbc-library.json` covering street food, new food cultures, food waste, fast food, future food, and comfort food. Updated the Food Around the World preset to use the new BBC series anchors for street food, cross-cultural food habits, and future food/food security while keeping World Flight and restaurant-language anchors where they are more specific. Evidence: focused Vitest suite (`course-presets`, `library-source-material`, `source-library`) passes; focused lint on changed TS test/source files passes; `pnpm exec tsc --noEmit --pretty false` passes.

C5/C6 health batch note (Jul 9, Codex local WIP): added six BBC Learning English / 6 Minute English health and wellbeing sources to `src/data/bbc-library.json` covering sleep, stress, sitting/movement, flourishing, workplace mental health, and kindness. Updated the Health & Wellbeing preset to use short BBC video anchors for sitting/movement and flourishing while keeping the stronger TED-Ed science anchors for sleep, stress, sugar, and gut health. Evidence: focused Vitest suite (`course-presets`, `library-source-material`, `source-library`) passes; focused lint on changed TS test/source files passes; `pnpm exec tsc --noEmit --pretty false` passes.

C5/C6 technology batch note (Jul 9, Codex local WIP): added six BBC Learning English / 6 Minute English technology sources to `src/data/bbc-library.json` covering AI training, robot companions, the future of work, AI call centres, improving the web, and distrust/fake news. Updated the Technology & AI preset to use BBC video anchors for AI, future work, web/digital life, customer-service AI, and media literacy while keeping the TED innovation source. Evidence: focused Vitest suite (`course-presets`, `library-source-material`, `source-library`) passes; focused lint on changed TS test/source files passes; `pnpm exec tsc --noEmit --pretty false` passes.

C5/C6 climate batch note (Jul 9, Codex local WIP): added six BBC Learning English / 6 Minute English climate and environment sources to `src/data/bbc-library.json` covering climate anxiety, plastic-free living, water scarcity, recycling systems, animal migration, and population/consumption. Updated the Climate & Environment preset to use BBC video anchors for climate mental health, population/consumption, plastic, and animal migration while keeping World Flight city-adaptation anchors for Cape Town water scarcity and Jakarta flood control. Evidence: focused Vitest suite (`course-presets`, `library-source-material`, `source-library`) passes; focused lint on changed TS test/source files passes; `pnpm exec tsc --noEmit --pretty false` passes.

C5/C6 animals batch note (Jul 9, Codex local WIP): added six BBC Learning English / 6 Minute English animal and nature sources to `src/data/bbc-library.json` covering zoos/conservation, pets, octopus intelligence, ecotourism, animal coloration/adaptation, and human-vs-animal efficiency. Updated the Animals & Nature preset to use BBC anchors for migration, octopus intelligence, zoo ethics/conservation, and pets, while keeping the strong NatGeo anchors for wolves/ecosystems and elephant memory. Evidence: focused Vitest suite (`course-presets`, `library-source-material`, `source-library`) passes; focused lint on changed TS test/source files passes; `pnpm exec tsc --noEmit --pretty false` passes.

C5/C6 stories batch note (Jul 9, Codex local WIP): added six TED-Ed story/myth sources to `src/data/teded-library.json` covering Prometheus, Orpheus and Eurydice, Sisyphus, King Midas, Arachne, and fairy-tale origins/adaptation. Updated the Stories, Myths & Folktales preset to use TED-Ed video anchors for myths/morals, fairy-tale versions, and creative retelling while keeping the strong existing storytelling, single-story, and hero-journey anchors. Evidence: focused Vitest suite (`course-presets`, `library-source-material`, `source-library`) passes; focused lint on changed TS test/source files passes; `pnpm exec tsc --noEmit --pretty false` passes.

C6 continuity note (Jul 9, Codex): newly saved Course Builder lessons now carry a compact `courseContext` inside the existing lesson payload JSON: course title/theme, lesson number, previous lesson topics, and deduplicated prior keywords. The lesson generation route appends this as bounded continuity guidance so later course lessons can naturally recycle earlier vocabulary without changing the current topic or overriding source-grounding. Files: `src/lib/course-context.ts`, `src/lib/course.ts`, `src/lib/planner-utils.ts`, `src/components/course/course-builder.tsx`, `src/hooks/use-lesson-session.ts`, `src/activities/types.ts`, `src/app/api/lesson-plan/generate/route.ts`, `src/lib/course-context.test.ts`, `src/lib/course-payload.test.ts`. Evidence: focused Vitest suite (`course-context`, `course-payload`, `course-outline`) passes; touched-file ESLint passes; `pnpm exec tsc --noEmit --pretty false` passes.

C6 source-briefing visibility note (Jul 9, Codex): course detail lesson cards now surface the saved grounding mode for each lesson — video briefing, reading briefing, or topic-grounded — with the resolved source title, a short briefing/summary preview, and any course-continuity review terms. This makes it obvious before launch what will ground the lesson and whether previous-course vocabulary is being carried forward. Files: `src/lib/course-briefing.ts`, `src/lib/course-briefing.test.ts`, `src/components/course/course-detail.tsx`, `docs/beta-readiness-checklist.md`. Evidence: focused Vitest suite (`course-briefing`, `library-source-material`, `course-payload`) passes; touched-file Next lint passes; `pnpm exec tsc --noEmit --pretty false` passes.

C6 source hydration note (Jul 9, Codex): course lesson serialization now rehydrates missing `lessonPayload.sourceMaterial` from `source_ref` on reads, so older saved courses and template rows with a library source still display and launch as source-grounded even if their stored payload predates source material embedding. Existing payload source material is preserved. Files: `src/lib/course-serialize.ts`, `src/lib/course-serialize.test.ts`, `docs/beta-readiness-checklist.md`. Evidence: focused Vitest suite (`course-serialize`, `course-briefing`, `library-source-material`, `course-payload`) passes; touched-file Next lint passes; `pnpm exec tsc --noEmit --pretty false` passes.

C6 preset integrity note (Jul 9, Codex local WIP): course preset tests now assemble every preset lesson through the same Course Builder payload path and assert valid/hydrated library sources, correct video/read-aloud briefing routing, saved flight metadata, continuity context, no accidental Grammar preset routing, and at least one concrete keyword echoed by the source title/summary. The audit surfaced one weak keyword set in Stories/Myths ("Hero Patterns"), now aligned to `hero journey` / `narrative pattern` from the TED-Ed source. Files: `src/lib/course-presets.test.ts`, `src/lib/course-presets.ts`, `docs/beta-readiness-checklist.md`. Evidence: focused Vitest suite (`course-presets`, `course-payload`, `course-context`, `library-source-material`, `source-library`) passes; touched-file Next lint passes; `pnpm exec tsc --noEmit --pretty false` passes.

C6 preset metadata-hardening note (Jul 9, Codex local WIP): added a local JSON quality gate for every source currently used by course presets: useful tags, non-empty difficulty, useful summary/description grounding, video duration/YouTube metadata, reading word counts where applicable, duplicate-tag checks, and keyword-to-metadata alignment. Enriched sparse preset-used metadata in Travel English, TED, TED-Ed, and BBC entries (airport travel, gut/sugar/sleep/stress/hero/single-story/innovation/Midas/future-work anchors). Files: `src/lib/course-presets.test.ts`, `src/data/travel-english-library.json`, `src/data/teded-library.json`, `src/data/ted-library.json`, `src/data/bbc-library.json`, `docs/beta-readiness-checklist.md`. Evidence: `pnpm vitest run src/lib/course-presets.test.ts src/lib/course-payload.test.ts src/lib/course-context.test.ts src/lib/library-source-material.test.ts src/lib/source-library.test.ts` passes (41 tests); `pnpm next lint --file src/lib/course-presets.ts --file src/lib/course-presets.test.ts` passes; `pnpm exec tsc --noEmit --pretty false` passes.
